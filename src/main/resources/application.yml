server:
  port: 8082

spring:
  jpa:
    hibernate:
      ddl-auto: validate
    show-sql: false
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
        format_sql: true

  flyway:
    enabled: true
    locations: classpath:db/migration

  cache:
    type: caffeine
    caffeine:
      spec: maximumSize=500,expireAfterWrite=3600s

openai:
  api-key: ${OPENAI_API_KEY}
  embedding-model: ${OPENAI_EMBEDDING_MODEL:text-embedding-3-small}
  base-url: https://api.openai.com

similar:
  minimum-vector-similarity: 0.55
  strong-vector-similarity: 0.72
  minimum-final-score: 0.52
  fallback-vector-similarity: 0.44
  fallback-final-score: 0.42
  last-resort-vector-similarity: 0.37
  minimum-keyword-overlap: 0.12
  minimum-title-overlap: 0.2
  lexical-strict-score: 0.46
  max-per-blog: 2
  strict:
    vector: 0.62
    title-overlap: 0.16
    body-overlap: 0.08
    topic-overlap: 0.08
    phrase-score: 0.06
  fallback:
    vector-dominant: 0.75
    lexical-dominant: 0.88
  lexical:
    title-overlap: 0.34
    body-overlap: 0.2
    topic-overlap: 0.3
    phrase-score: 0.16

admin:
  api-key: ${ADMIN_API_KEY}

management:
  endpoints:
    web:
      exposure:
        include: health,info,prometheus
  endpoint:
    health:
      show-details: always
